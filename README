1. clone
cd && git clone https://github.com/fpauser/vim-config
cd vim-config && git submodule init && git submodule update

2. setup
mkdir ~/.vim_swap
ln -sf ~/vim-config/.vim/bundle ~/.vim/
ln -sf ~/vim-config/.vimrc ~/
ln -sf ~/vim-config/.gvimrc ~/
